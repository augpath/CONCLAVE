services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - conclave_gui_data:/data
    environment:
      - CONCLAVE_GUI_DATA=/data

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  conclave_gui_data:
